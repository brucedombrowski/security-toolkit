# Security scan results (never commit detailed findings)
.scans/
*-Scan-Results.md

# Controlled Unclassified Information (CUI) outputs
CUI*.*
*CUI*
*-CUI-*

# Personally Identifiable Information (PII) outputs
PII*.*
*PII*
*-PII-*

# Host inventory (contains sensitive system information)
host-inventory*.txt
*-inventory-*.txt

# Allowlists (project-specific reviewed exceptions - should not be shared)
.allowlists/

# Security assessments & remediation plans (internal use only)
.assessments/

# Verification package outputs (generated PDFs - attach to releases)
.verification/

# Vulnerability scan outputs (SENSITIVE - network topology, CVEs)
nmap-*.txt
nmap-*.xml
lynis-*.txt
lynis-*.dat
openvas-*.xml
vulnerability-scan-*.txt

# Secrets and credentials (CRITICAL - never commit)
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
.env
.env.*
*.secrets
secrets.*
credentials.*
*.credential
api_key*
apikey*

# SSH keys
id_rsa*
id_ed25519*
id_ecdsa*
id_dsa*
*.pub
known_hosts
authorized_keys

# GPG keys
*.gpg
*.asc
secring.*
pubring.*

# AWS credentials
.aws/
aws_access_key*
aws_secret*

# Database dumps (may contain sensitive data)
*.sql
*.dump
*.bak

# Log files (may contain sensitive data)
*.log
logs/

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# Claude Code configuration (user-specific)
.claude/

# Threat intelligence cache (KEV catalog, IOCs)
.cache/

# Demo/test environment outputs
demo/**/scan-results/
demo/**/.scans/
demo/**/.vagrant/
demo/**/**.log

# Scanner config (contains target IPs and meeting URLs)
demo_scanner.conf

# Homebrew tap (separate repo)
homebrew-tap/
